using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class DebugExtensions
{
    // public static void DebugPoint(this Vector3 position)
    // {
    //     Debug.DrawRay(position, position + Vector3.right, Color.red);
    //     Debug.DrawRay(position, position + Vector3.up, Color.green);
    //     Debug.DrawRay(position, position + Vector3.forward, Color.blue);
    // }
}
